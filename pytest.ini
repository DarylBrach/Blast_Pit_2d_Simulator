[pytest]
markers =
    docker_host: uses the shared governed Docker daemon and is serialized across test processes
    governed_host: requires the exact authorization-bound Codex CLI, Docker host, and attested image
    windows_acl: requires native Windows PowerShell ACL capability
